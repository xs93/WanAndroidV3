<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="DynamicColorTheme" parent="Theme.Material3.DynamicColors.DayNight">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">?colorSurface</item>
    </style>

    <style name="AppBaseTheme" parent="Theme.Material3.Light">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="materialButtonStyle">@style/WanCommonButtonStyle</item>
        <item name="toolbarStyle">@style/Widget.App.Toolbar</item>
        <item name="switchStyle">@style/Widget.App.Switch</item>
        <item name="materialCardViewStyle">@style/Widget.App.CardView</item>
        <item name="bottomSheetDialogTheme">@style/ThemeOverlay.App.BottomSheetDialog</item>

    </style>

    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:windowLightStatusBar">true</item>
        <!-- 关闭阴影效果带来的Material控件背景色被改变导致的不一致 -->
        <item name="elevationOverlayEnabled">false</item>
        <item name="colorPrimary">@color/md_theme_light_primary</item>
        <item name="colorPrimaryContainer">@color/white</item>
        <item name="colorAccent">?colorPrimary</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorSurfaceContainer">@color/white</item>
        <item name="android:colorBackground">@color/color_fafafa</item>
    </style>

    <style name="AppTheme.FullScreen">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="AppTheme.Transparent">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="AppTheme.Immersive">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@color/transparent</item>
    </style>
</resources>